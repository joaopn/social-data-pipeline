# Social Data Pipeline - GPU Dependencies
# Used by: ml profile (transformer classifiers)
#
# Base image (nvcr.io/nvidia/pytorch) already includes:
#   numpy, scipy, transformers, torch, cuda libs

--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/

pyyaml>=6.0
polars>=1.0.0
optimum[onnxruntime-gpu]
