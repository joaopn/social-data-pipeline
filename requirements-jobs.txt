# Social Data Pipeline - Query Scheduler (jobs profile)
# Used by: jobs profile

# Web / MCP server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
# 2.x API: `mcp.server.mcpserver.MCPServer` (the 1.x `mcp.server.fastmcp`
# module was removed), with transport options passed to streamable_http_app().
mcp>=2.0.0

# Config
pyyaml>=6.0

# SQL pretty-printing for the UI
sqlparse>=0.5.0

# Parquet preview of completed job results in the UI
pyarrow>=17.0.0

# Database clients (PostgreSQL + StarRocks + MongoDB)
psycopg[binary]>=3.1.0
mysql-connector-python>=9.0.0
pymongo>=4.6.0
