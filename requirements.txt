# Reddit Data Tools - CPU Dependencies
# Used by: parse, ml_cpu, postgres_ingest, postgres_ml profiles

# Core
pyyaml>=6.0
polars>=1.0.0

# Language detection (ml_cpu profile)
lingua-language-detector>=2.0.0

# PostgreSQL (postgres_ingest, postgres_ml profiles)
psycopg[binary]>=3.1.0
